# Replica de variáveis do Render para ambiente local
# Este arquivo NÃO deve conter segredos de produção.

# Diretório de dados gravável para SQLite e uploads
DATA_DIR=.\backend\data

# Flags de bootstrap/migração (mantidas desativadas)
MIGRATE_PG_TO_SQLITE=0
BOOTSTRAP_FROM_SQLITE=0
BOOTSTRAP_FORCE=0

# Seeds de usuários/clientes para dev (igual ao Render)
SEED_ADMIN=1
SEED_WILLIANS=1
SEED_LOJAS=1

# CORS e afins
ALLOW_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3005
APP_VERSION=1.0.0
SECRET_KEY=secret-key-thors-gestor-2025
